# Part of smarterp. See LICENSE file for full copyright and licensing details.

from smarterp import fields, models


class HrContract(models.Model):
    _inherit = 'hr.payslip'

    l10n_{{code}}_field = fields.Char()
